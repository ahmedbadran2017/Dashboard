# Python
__pycache__/
*.pyc
*.egg-info/
.eggs/

# Built SPA bundle IS committed (ops_dashboard/public/ops_dashboard.bundle.js/.css)
# so the production bench serves the PWA without running Node.
# Rebuild with `cd frontend && npm run build` before each deploy commit.

# Node
frontend/node_modules/
frontend/dist/
npm-debug.log*

# Env / editor
.env
.env.local
.DS_Store
.vscode/
.idea/
